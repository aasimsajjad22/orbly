framework:
    validation:
        # Read validation rules from #[Assert\...] attributes on your classes.
        # Without this, your DTO and entity constraints are ignored.
        email_validation_mode: html5

when@test:
    framework:
        validation:
            not_compromised_password: false   # skip the "have i been pwned" API call in tests
