memory_limit=256M
max_execution_time=30

; Do not leak stack traces to users. Errors go to stderr, which Docker
; captures as container logs.
display_errors=Off
log_errors=On
error_log=/dev/stderr

expose_php=Off

date.timezone=UTC

realpath_cache_size=4096K
realpath_cache_ttl=600
